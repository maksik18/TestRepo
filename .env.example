# Copy this file to `.env` and adjust the values.
# All vars are optional; sensible defaults live in config.py.
BASE_URL=
USERNAME=
PASSWORD=

# Explicit wait timeout ( seconds )
DRIVER_TIMEOUT=10

# Run Chrome in headed mode for local debugging: false
HEADLESS=true
